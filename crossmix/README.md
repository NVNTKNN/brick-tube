# CrossMix packaging

`config.json` goes into `Apps/BrickTube/` on a CrossMix-OS card, next to the
same `launch.sh` + `bricktube-core.sh` the NextUI pak uses. The manifest schema
is unverified on real hardware (beta) — if CrossMix doesn't list the app,
this file is the first thing to fix.
